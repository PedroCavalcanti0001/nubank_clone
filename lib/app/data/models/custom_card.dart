class CustomCard {
  String title;
  String text;
  String buttonText;
  Function buttonAction;
  bool isNew;

  CustomCard(
      this.title, this.text, this.buttonText, this.buttonAction, this.isNew);
}
